# Package file audit (0/1 ship rating)

Generated by `npm run audit:package-files` (run after `npm run build` for accurate dist ratings).

## How to read

| `required` | Meaning |
|-------------|---------|
| **1** | Must be included in the Chrome Web Store / public extension zip |
| **0** | Dev-only or build-only — must **not** be in the public zip |

The public package is **only** the contents of `dist/` after `npm run build`. Everything else in the repo is `required=0`.

Full file-by-file list: [PACKAGE-FILE-AUDIT.csv](./PACKAGE-FILE-AUDIT.csv)

## Summary

| Metric | Count |
|--------|------:|
| Total paths audited | 224 |
| Dist runtime (`required=1`) | 24 |
| Dist bloat (`required=0`, exclude from zip) | 3 |
| Repo dev-only (`required=0`) | 197 |

## Dist runtime — ship these (`required=1`)

| Path | Notes |
|------|-------|
| `dist/assets/animation.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/htmlEscape.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/index.css` | reachable from manifest/HTML/JS closure |
| `dist/assets/index.html.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/index.html2.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/index.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/index.ts.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/index2.css` | reachable from manifest/HTML/JS closure |
| `dist/assets/playerProgress.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/welcome.css` | reachable from manifest/HTML/JS closure |
| `dist/assets/welcome.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/welcomeConfig.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/welcomePage.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/youtube-content.bundle.js` | reachable from manifest/HTML/JS closure |
| `dist/assets/youtubeMeta.js` | reachable from manifest/HTML/JS closure |
| `dist/icons/icon-128.png` | reachable from manifest/HTML/JS closure |
| `dist/icons/icon-16.png` | reachable from manifest/HTML/JS closure |
| `dist/icons/icon-32.png` | reachable from manifest/HTML/JS closure |
| `dist/icons/icon-48.png` | reachable from manifest/HTML/JS closure |
| `dist/manifest.json` | reachable from manifest/HTML/JS closure |
| `dist/service-worker-loader.js` | reachable from manifest/HTML/JS closure |
| `dist/src/dashboard/index.html` | reachable from manifest/HTML/JS closure |
| `dist/src/popup/index.html` | reachable from manifest/HTML/JS closure |
| `dist/src/welcome/index.html` | reachable from manifest/HTML/JS closure |

## Dist bloat — do not ship (`required=0`)

| Path | Notes |
|------|-------|
| `dist/assets/youtube-content.bundle.js.map` | in dist/ but not referenced at runtime — exclude from store zip |
| `dist/icons/icon-96.png` | in dist/ but not referenced at runtime — exclude from store zip |
| `dist/icons/logo-source.png` | in dist/ but not referenced at runtime — exclude from store zip |

## Repo dev-only by category (`required=0`)

| Category | Files | Description |
|----------|------:|-------------|
| asset-dev | 16 | see CSV |
| config | 9 | see CSV |
| docs | 13 | see CSV |
| other-dev | 1 | see CSV |
| public-input | 8 | see CSV |
| script | 13 | see CSV |
| source | 96 | see CSV |
| test | 41 | see CSV |

## Regenerate

```bash
npm run build && npm run audit:package-files
```
